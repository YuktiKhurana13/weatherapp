import React from 'react';

class Title extends React.Component{
    render(){
        return(
            <div>
                Title component
            </div>
        );
    }
}

export default Title;